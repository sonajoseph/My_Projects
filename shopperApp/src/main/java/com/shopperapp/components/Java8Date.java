package com.shopperapp.components;

import java.time.LocalDate;
import java.time.LocalDateTime;
import java.time.LocalTime;
import java.time.temporal.ChronoUnit;

import org.bson.codecs.jsr310.LocalDateTimeCodec;

public class Java8Date {
	
	/*
	 * public static void main(String [] args) { LocalDate localDate =
	 * LocalDate.now(); System.err.println("localDate : "+localDate);
	 * 
	 * 
	 * LocalTime localTime = LocalTime.now();
	 * System.err.println("localTime : "+localTime);
	 * 
	 * System.err.println("localDate minus 1 month : "+localDate.minus(1,
	 * ChronoUnit.DAYS)); LocalDateTime localDateTime = LocalDateTime.now();
	 * System.err.println("localDateTime : "+localDateTime);
	 * 
	 * System.err.println("localDateTime minus 1 Day : "+localDateTime.minus(1,
	 * ChronoUnit.DAYS));
	 * 
	 * 
	 * System.err.println("localDateTime Plus 1 Day : "+localDateTime.plus(1,
	 * ChronoUnit.YEARS));
	 * 
	 * 
	 * 
	 * }
	 */

}
